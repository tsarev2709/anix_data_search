# Production deployment

The dashboard is deployed from `main` through GitHub Actions. Browser configuration contains only the Supabase URL and publishable key; privileged credentials remain in GitHub and Supabase secrets.
